Django je webový framework  

Frameworky x knihovny


MVC architektura
model, view, controller

MTV
model, template, view 

DTL = django template language

v šablonách existují tagy a filtry


statický - zůstává stejný (pro nás CSS, JavaScript)
dynamický - mění se (pro nás Django-verze HTML)